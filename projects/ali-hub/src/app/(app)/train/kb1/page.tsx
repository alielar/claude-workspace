"use client";

/** /train/kb1 · Kettlebell 30: the 11-move round, AMRAP 30 min, no tap per movement. */

import { AmrapGame } from "@/components/train/AmrapGame";

export default function Kb1Page() {
  return <AmrapGame workoutKey="kb1" />;
}
