"use client";

/** /train/w1 · Workout 1: the original AMRAP. The game itself lives in AmrapGame. */

import { AmrapGame } from "@/components/train/AmrapGame";

export default function W1Page() {
  return <AmrapGame workoutKey="w1" />;
}
